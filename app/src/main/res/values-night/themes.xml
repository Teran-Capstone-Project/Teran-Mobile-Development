<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Teran" parent="Theme.Material3.DayNight">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorAccent">@color/strong_helper_text_color</item>
    </style>

    <!-- BUTTON  -->
    <style name="StrokeBorderGrayButton">
        <item name="strokeColor">@color/stroke_border_dark</item>
        <item name="strokeWidth">1dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/dark</item>
    </style>
</resources>